{
  "schemaVersion": 1,
  "id": "${configApiModId}",
  "version": "${version}",

  "name": "Mezz Config API",
  "description": "Shared config API for Minecraft mods.",
  "authors": [
    "${modAuthor}"
  ],
  "contact": {
    "sources": "${githubUrl}"
  },

  "license": "The MIT License (MIT)",

  "environment": "*",

  "depends": {
    "fabricloader": "${fabricLoaderVersionRange}",
    "fabric-api": "${fabricApiVersionRange}",
    "java": ">=${modJavaVersion}"
  }
}
