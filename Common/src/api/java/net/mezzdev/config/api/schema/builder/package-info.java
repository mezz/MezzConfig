/**
 * Declare schemas, categories, and their config values.
 * <p>
 * Obtain a schema builder from {@link net.mezzdev.config.api.IConfigRegistration}, then follow the returned builders until
 * every value and the schema have been built.
 */
@NullMarked
package net.mezzdev.config.api.schema.builder;

import org.jspecify.annotations.NullMarked;
