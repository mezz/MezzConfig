/**
 * Declare schemas, categories, and their config values.
 * <p>
 * Obtain a schema builder from {@link IConfigRegistration}, then follow the returned builders until every value and the
 * schema have been built.
 */
@NullMarked
package net.mezzdev.config.api.schema.builder;

import net.mezzdev.config.api.IConfigRegistration;
import org.jspecify.annotations.NullMarked;
